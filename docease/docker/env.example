# ============================================
# CONFIGURATION N8N - LOCAL (développement)
# ============================================
# Copiez ce fichier vers .env et modifiez les valeurs selon vos besoins
# IMPORTANT: Le fichier .env ne doit JAMAIS être commité dans git

# --------------------------------------------
# Configuration n8n de base
# --------------------------------------------
N8N_HOST=localhost
N8N_PORT=5678
N8N_PROTOCOL=http
N8N_EDITOR_BASE_URL=http://localhost:5678
WEBHOOK_URL=http://localhost:5678

# --------------------------------------------
# PostgreSQL (développement et production)
# --------------------------------------------
POSTGRES_DB=n8n
POSTGRES_USER=n8n
POSTGRES_PASSWORD=changez_moi_mot_de_passe_securise

# --------------------------------------------
# Authentification basique n8n
# Recommandé pour la production
# --------------------------------------------
N8N_BASIC_AUTH_ACTIVE=false
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=changez_moi

# --------------------------------------------
# Configuration générale
# --------------------------------------------
GENERIC_TIMEZONE=Europe/Paris
N8N_LOG_LEVEL=debug
N8N_DIAGNOSTICS_ENABLED=true

# --------------------------------------------
# Configuration CORS
# Permet les requêtes cross-origin
# Pour l'origine 'null' (fichiers locaux), utilisez '*'
# --------------------------------------------
N8N_CORS_ENABLED=true
N8N_CORS_ALLOW_ORIGIN=*

# ============================================
# CONFIGURATION N8N - PRODUCTION (VPS)
# ============================================
# Pour la production, modifiez ces valeurs :

# N8N_HOST=votre-domaine.com
# N8N_PORT=5678
# N8N_PROTOCOL=https
# N8N_EDITOR_BASE_URL=https://votre-domaine.com
# WEBHOOK_URL=https://votre-domaine.com

# ============================================
# CONFIGURATION CLOUDFLARE TUNNEL (Alternative)
# ============================================
# Si vous utilisez Cloudflare Tunnel au lieu de Caddy :
# N8N_HOST=n8n.votre-domaine.com
# N8N_PROTOCOL=https
# N8N_EDITOR_BASE_URL=https://n8n.votre-domaine.com
# WEBHOOK_URL=https://n8n.votre-domaine.com
# 
# Voir docs/CONFIGURER_CLOUDFLARE_TUNNEL.md pour la configuration complète
# Script d'aide : scripts/setup-cloudflare-tunnel.ps1

# Les mêmes variables PostgreSQL s'appliquent (voir ci-dessus)
# Changez POSTGRES_PASSWORD pour un mot de passe fort en production

# N8N_BASIC_AUTH_ACTIVE=true
# N8N_BASIC_AUTH_USER=admin
# N8N_BASIC_AUTH_PASSWORD=mot_de_passe_securise

# GENERIC_TIMEZONE=Europe/Paris
# N8N_LOG_LEVEL=info
# N8N_DIAGNOSTICS_ENABLED=false

# ============================================
# CONFIGURATION EMAIL (Optionnel)
# ============================================
# SMTP pour les emails (Gmail, Outlook, etc.)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=votre-email@gmail.com
# SMTP_PASSWORD=votre_mot_de_passe_app
# SMTP_SECURE=false

# Email de validation (recevra le document à approuver)
# VALIDATION_EMAIL=prenom.nom@entreprise.com

# ============================================
# CONFIGURATION IA LOCALE (Optionnel)
# ============================================
# LM Studio ou Ollama
# IA_HOST=localhost
# IA_PORT=1234
# IA_MODEL=llama-2-7b-chat

# ============================================
# NOTES IMPORTANTES
# ============================================
# 1. En production, utilisez des mots de passe forts
# 2. Activez toujours l'authentification basique en production
# 3. Configurez HTTPS via Caddy OU Cloudflare Tunnel
#    - Caddy : Nécessite un domaine avec DNS configuré
#    - Cloudflare Tunnel : Pas besoin d'ouvrir de ports, gratuit
# 4. Changez tous les mots de passe par défaut
# 5. Ne partagez jamais votre fichier .env
# 6. Si vous utilisez Cloudflare Tunnel, vous n'avez pas besoin de Caddy

